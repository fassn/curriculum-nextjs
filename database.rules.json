{
  "rules": {
    ".read": "auth == null",
    ".write": "auth == null",
    "messages": {
      ".indexOn": ["timestamp"],
      "$messageId": {
        ".validate": "newData.hasChildren(['email', 'title', 'message', 'timestamp'])"
      }
    }
  }
}